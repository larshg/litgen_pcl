#include <pybind11/pybind11.h>
#include <pybind11/stl.h>
#include <pybind11/functional.h>
#include <pybind11/numpy.h>

#include <pcl/types.h>
#include <pcl/point_types.h>
#include <pcl/point_cloud.h>

namespace py = pybind11;


// !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!  AUTOGENERATED CODE !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
// <litgen_glue_code>  // Autogenerated code below! Do not edit!

// </litgen_glue_code> // Autogenerated code end
// !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!  AUTOGENERATED CODE END !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!



void py_init_module_pcl(py::module& m)
{
    // using namespace pcl;  // NON!

    // !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!  AUTOGENERATED CODE !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
    // <litgen_pydef> // Autogenerated code below! Do not edit!
    ////////////////////    <generated_from:point_types.h>    ////////////////////

    { // <namespace pcl>
        py::module_ pyNspcl = m.def_submodule("pcl", "* @{");
        auto pyEnumBorderTrait =
            py::enum_<pcl::BorderTrait>(pyNspcl, "BorderTrait", py::arithmetic(), "* \\brief Specification of the fields for BorderDescription::traits.\n    * \\ingroup common\n")
                .value("border_trait_obstacle_border", pcl::BORDER_TRAIT__OBSTACLE_BORDER, "")
                .value("border_trait_shadow_border", pcl::BORDER_TRAIT__SHADOW_BORDER, "")
                .value("border_trait_veil_point", pcl::BORDER_TRAIT__VEIL_POINT, "")
                .value("border_trait_shadow_border_top", pcl::BORDER_TRAIT__SHADOW_BORDER_TOP, "")
                .value("border_trait_shadow_border_right", pcl::BORDER_TRAIT__SHADOW_BORDER_RIGHT, "")
                .value("border_trait_shadow_border_bottom", pcl::BORDER_TRAIT__SHADOW_BORDER_BOTTOM, "")
                .value("border_trait_shadow_border_left", pcl::BORDER_TRAIT__SHADOW_BORDER_LEFT, "")
                .value("border_trait_obstacle_border_top", pcl::BORDER_TRAIT__OBSTACLE_BORDER_TOP, "")
                .value("border_trait_obstacle_border_right", pcl::BORDER_TRAIT__OBSTACLE_BORDER_RIGHT, "")
                .value("border_trait_obstacle_border_bottom", pcl::BORDER_TRAIT__OBSTACLE_BORDER_BOTTOM, "")
                .value("border_trait_obstacle_border_left", pcl::BORDER_TRAIT__OBSTACLE_BORDER_LEFT, "")
                .value("border_trait_veil_point_top", pcl::BORDER_TRAIT__VEIL_POINT_TOP, "")
                .value("border_trait_veil_point_right", pcl::BORDER_TRAIT__VEIL_POINT_RIGHT, "")
                .value("border_trait_veil_point_bottom", pcl::BORDER_TRAIT__VEIL_POINT_BOTTOM, "")
                .value("border_trait_veil_point_left", pcl::BORDER_TRAIT__VEIL_POINT_LEFT, "");
    } // </namespace pcl>
    ////////////////////    </generated_from:point_types.h>    ////////////////////


    ////////////////////    <generated_from:point_types.hpp>    ////////////////////

    { // <namespace pcl>
        py::module_ pyNspcl = m.def_submodule("pcl", "namespace pcl");
        auto pyNspcl_Class_PointXYZ =
            py::class_<pcl::_PointXYZ>
                (pyNspcl, "_PointXYZ", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointXYZ =
            py::class_<pcl::PointXYZ, pcl::_PointXYZ>
                (pyNspcl, "PointXYZ", "* \\brief A point structure representing Euclidean xyz coordinates. (SSE friendly)\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointXYZ &>(),
                py::arg("p"))
            .def(py::init<>())
            .def(py::init<float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"))
            ;


        auto pyNspcl_Class_RGB =
            py::class_<pcl::_RGB>
                (pyNspcl, "_RGB", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassRGB =
            py::class_<pcl::RGB, pcl::_RGB>
                (pyNspcl, "RGB", "* \\brief A structure representing RGB color information.\n    *\n    * The RGBA information is available either as separate r, g, b, or as a\n    * packed std::uint32_t rgba value. To pack it, use:\n    *\n    * \\code\n    * int rgb = ((int)r) << 16 | ((int)g) << 8 | ((int)b);\n    * \\endcode\n    *\n    * To unpack it use:\n    *\n    * \\code\n    * int rgb = ...;\n    * std::uint8_t r = (rgb >> 16) & 0x0000;\n    * std::uint8_t g = (rgb >> 8)  & 0x0000;\n    * std::uint8_t b = (rgb)     & 0x0000;\n    * \\endcode\n    *\n")
            .def(py::init<const pcl::_RGB &>(),
                py::arg("p"))
            .def(py::init<>())
            .def(py::init<std::uint8_t, std::uint8_t, std::uint8_t, std::uint8_t>(),
                py::arg("_r"), py::arg("_g"), py::arg("_b"), py::arg("_a") = 255)
            ;


        auto pyNspcl_Class_Intensity =
            py::class_<pcl::_Intensity>
                (pyNspcl, "_Intensity", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassIntensity =
            py::class_<pcl::Intensity, pcl::_Intensity>
                (pyNspcl, "Intensity", "* \\brief A point structure representing the grayscale intensity in single-channel images.\n    * Intensity is represented as a float value.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_Intensity &>(),
                py::arg("p"))
            .def(py::init<float>(),
                py::arg("_intensity") = 0.f)
            ;


        auto pyNspcl_Class_Intensity8u =
            py::class_<pcl::_Intensity8u>
                (pyNspcl, "_Intensity8u", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassIntensity8u =
            py::class_<pcl::Intensity8u, pcl::_Intensity8u>
                (pyNspcl, "Intensity8u", "* \\brief A point structure representing the grayscale intensity in single-channel images.\n    * Intensity is represented as a std::uint8_t value.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_Intensity8u &>(),
                py::arg("p"))
            .def(py::init<std::uint8_t>(),
                py::arg("_intensity") = 0)
            ;


        auto pyNspcl_Class_Intensity32u =
            py::class_<pcl::_Intensity32u>
                (pyNspcl, "_Intensity32u", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassIntensity32u =
            py::class_<pcl::Intensity32u, pcl::_Intensity32u>
                (pyNspcl, "Intensity32u", "* \\brief A point structure representing the grayscale intensity in single-channel images.\n    * Intensity is represented as a std::uint32_t value.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_Intensity32u &>(),
                py::arg("p"))
            .def(py::init<std::uint32_t>(),
                py::arg("_intensity") = 0)
            ;


        auto pyNspcl_Class_PointXYZI =
            py::class_<pcl::_PointXYZI>
                (pyNspcl, "_PointXYZI", "* \\brief A point structure representing Euclidean xyz coordinates, and the intensity value.\n    * \\ingroup common\n")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointXYZI =
            py::class_<pcl::PointXYZI, pcl::_PointXYZI>
                (pyNspcl, "PointXYZI", "")
            .def(py::init<const pcl::_PointXYZI &>(),
                py::arg("p"))
            .def(py::init<float>(),
                py::arg("_intensity") = 0.f)
            .def(py::init<float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_intensity") = 0.f)
            ;


        auto pyNspcl_Class_PointXYZL =
            py::class_<pcl::_PointXYZL>
                (pyNspcl, "_PointXYZL", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointXYZL =
            py::class_<pcl::PointXYZL, pcl::_PointXYZL>
                (pyNspcl, "PointXYZL", "")
            .def(py::init<const pcl::_PointXYZL &>(),
                py::arg("p"))
            .def(py::init<std::uint32_t>(),
                py::arg("_label") = 0)
            .def(py::init<float, float, float, std::uint32_t>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_label") = 0)
            ;


        auto pyNspcl_ClassLabel =
            py::class_<pcl::Label>
                (pyNspcl, "Label", "")
            .def_readwrite("label", &pcl::Label::label, "")
            .def(py::init<std::uint32_t>(),
                py::arg("_label") = 0)
            ;


        auto pyNspcl_Class_PointXYZRGBA =
            py::class_<pcl::_PointXYZRGBA>
                (pyNspcl, "_PointXYZRGBA", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointXYZRGBA =
            py::class_<pcl::PointXYZRGBA, pcl::_PointXYZRGBA>
                (pyNspcl, "PointXYZRGBA", "* \\brief A point structure representing Euclidean xyz coordinates, and the RGBA color.\n    *\n    * The RGBA information is available either as separate r, g, b, or as a\n    * packed std::uint32_t rgba value. To pack it, use:\n    *\n    * \\code\n    * int rgb = ((int)r) << 16 | ((int)g) << 8 | ((int)b);\n    * \\endcode\n    *\n    * To unpack it use:\n    *\n    * \\code\n    * int rgb = ...;\n    * std::uint8_t r = (rgb >> 16) & 0x0000;\n    * std::uint8_t g = (rgb >> 8)  & 0x0000;\n    * std::uint8_t b = (rgb)     & 0x0000;\n    * \\endcode\n    *\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointXYZRGBA &>(),
                py::arg("p"))
            .def(py::init<>())
            .def(py::init<std::uint8_t, std::uint8_t, std::uint8_t, std::uint8_t>(),
                py::arg("_r"), py::arg("_g"), py::arg("_b"), py::arg("_a"))
            .def(py::init<float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"))
            .def(py::init<float, float, float, std::uint8_t, std::uint8_t, std::uint8_t, std::uint8_t>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_r"), py::arg("_g"), py::arg("_b"), py::arg("_a"))
            ;


        auto pyNspcl_Class_PointXYZRGB =
            py::class_<pcl::_PointXYZRGB>
                (pyNspcl, "_PointXYZRGB", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_Class_PointXYZRGBL =
            py::class_<pcl::_PointXYZRGBL>
                (pyNspcl, "_PointXYZRGBL", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointXYZRGB =
            py::class_<pcl::PointXYZRGB, pcl::_PointXYZRGB>
                (pyNspcl, "PointXYZRGB", "* \\brief A point structure representing Euclidean xyz coordinates, and the RGB color.\n    *\n    * Due to historical reasons (PCL was first developed as a ROS package), the\n    * RGB information is packed into an integer and casted to a float. This is\n    * something we wish to remove in the near future, but in the meantime, the\n    * following code snippet should help you pack and unpack RGB colors in your\n    * PointXYZRGB structure:\n    *\n    * \\code\n    * // pack r/g/b into rgb\n    * std::uint8_t r = 255, g = 0, b = 0;    // Example: Red color\n    * std::uint32_t rgb = ((std::uint32_t)r << 16 | (std::uint32_t)g << 8 | (std::uint32_t)b);\n    * p.rgb = *reinterpret_cast<float*>(&rgb);\n    * \\endcode\n    *\n    * To unpack the data into separate values, use:\n    *\n    * \\code\n    * PointXYZRGB p;\n    * // unpack rgb into r/g/b\n    * std::uint32_t rgb = *reinterpret_cast<int*>(&p.rgb);\n    * std::uint8_t r = (rgb >> 16) & 0x0000;\n    * std::uint8_t g = (rgb >> 8)  & 0x0000;\n    * std::uint8_t b = (rgb)       & 0x0000;\n    * \\endcode\n    *\n    *\n    * Alternatively, from 1.1.0 onwards, you can use p.r, p.g, and p.b directly.\n    *\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointXYZRGB &>(),
                py::arg("p"))
            .def(py::init<>())
            .def(py::init<std::uint8_t, std::uint8_t, std::uint8_t>(),
                py::arg("_r"), py::arg("_g"), py::arg("_b"))
            .def(py::init<float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"))
            .def(py::init<float, float, float, std::uint8_t, std::uint8_t, std::uint8_t>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_r"), py::arg("_g"), py::arg("_b"))
            ;


        auto pyNspcl_ClassPointXYZRGBL =
            py::class_<pcl::PointXYZRGBL, pcl::_PointXYZRGBL>
                (pyNspcl, "PointXYZRGBL", "")
            .def(py::init<const pcl::_PointXYZRGBL &>(),
                py::arg("p"))
            .def(py::init<std::uint32_t>(),
                py::arg("_label") = 0)
            .def(py::init<std::uint8_t, std::uint8_t, std::uint8_t>(),
                py::arg("_r"), py::arg("_g"), py::arg("_b"))
            .def(py::init<float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"))
            .def(py::init<float, float, float, std::uint8_t, std::uint8_t, std::uint8_t, std::uint32_t, std::uint8_t>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_r"), py::arg("_g"), py::arg("_b"), py::arg("_label") = 0, py::arg("_a") = 255)
            ;


        auto pyNspcl_Class_PointXYZLAB =
            py::class_<pcl::_PointXYZLAB>
                (pyNspcl, "_PointXYZLAB", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointXYZLAB =
            py::class_<pcl::PointXYZLAB, pcl::_PointXYZLAB>
                (pyNspcl, "PointXYZLAB", "* \\brief A point structure representing Euclidean xyz coordinates, and the CIELAB color.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointXYZLAB &>(),
                py::arg("p"))
            .def(py::init<>())
            .def(py::init<float, float, float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_l"), py::arg("_a"), py::arg("_b"))
            ;


        auto pyNspcl_Class_PointXYZHSV =
            py::class_<pcl::_PointXYZHSV>
                (pyNspcl, "_PointXYZHSV", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointXYZHSV =
            py::class_<pcl::PointXYZHSV, pcl::_PointXYZHSV>
                (pyNspcl, "PointXYZHSV", "")
            .def(py::init<const pcl::_PointXYZHSV &>(),
                py::arg("p"))
            .def(py::init<>())
            .def(py::init<float, float, float>(),
                py::arg("_h"), py::arg("_s"), py::arg("_v"),
                " @TODO: Use strong types??\n This is a dangerous type, doesn't behave like others")
            .def(py::init<float, float, float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_h"), py::arg("_s"), py::arg("_v"))
            ;


        auto pyNspcl_ClassPointXY =
            py::class_<pcl::PointXY>
                (pyNspcl, "PointXY", "/** \\brief A 2D point structure representing Euclidean xy coordinates.\n    * \\ingroup common\n    */\n NOLINTBEGIN(modernize-use-default-member-init)")
            .def(py::init<float, float>(),
                py::arg("_x"), py::arg("_y"))
            .def(py::init<>())
            .def("get_vector2f_map",
                [](pcl::PointXY & self) { return self.getVector2fMap(); })
            .def("get_vector2f_map",
                [](pcl::PointXY & self) { return self.getVector2fMap(); })
            ;


        auto pyNspcl_ClassPointUV =
            py::class_<pcl::PointUV>
                (pyNspcl, "PointUV", "* \\brief A 2D point structure representing pixel image coordinates.\n    * \note We use float to be able to represent subpixels.\n    * \\ingroup common\n")
            .def_readwrite("u", &pcl::PointUV::u, "")
            .def_readwrite("v", &pcl::PointUV::v, "")
            .def(py::init<>())
            .def(py::init<float, float>(),
                py::arg("_u"), py::arg("_v"))
            ;


        auto pyNspcl_ClassInterestPoint =
            py::class_<pcl::InterestPoint>
                (pyNspcl, "InterestPoint", "/** \\brief A point structure representing an interest point with Euclidean xyz coordinates, and an interest value.\n    * \\ingroup common\n    */\n @TODO: inheritance trick like on other PointTypes")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_Class_Normal =
            py::class_<pcl::_Normal>
                (pyNspcl, "_Normal", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassNormal =
            py::class_<pcl::Normal, pcl::_Normal>
                (pyNspcl, "Normal", "* \\brief A point structure representing normal coordinates and the surface curvature estimate. (SSE friendly)\n    * \\ingroup common\n")
            .def(py::init<const pcl::_Normal &>(),
                py::arg("p"))
            .def(py::init<float>(),
                py::arg("_curvature") = 0.f)
            .def(py::init<float, float, float, float>(),
                py::arg("n_x"), py::arg("n_y"), py::arg("n_z"), py::arg("_curvature") = 0.f)
            ;


        auto pyNspcl_Class_Axis =
            py::class_<pcl::_Axis>
                (pyNspcl, "_Axis", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassAxis =
            py::class_<pcl::Axis, pcl::_Axis>
                (pyNspcl, "Axis", "* \\brief A point structure representing an Axis using its normal coordinates. (SSE friendly)\n    *  \\ingroup common\n")
            .def(py::init<const pcl::_Axis &>(),
                py::arg("p"))
            .def(py::init<>())
            .def(py::init<float, float, float>(),
                py::arg("n_x"), py::arg("n_y"), py::arg("n_z"))
            ;


        auto pyNspcl_Class_PointNormal =
            py::class_<pcl::_PointNormal>
                (pyNspcl, "_PointNormal", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointNormal =
            py::class_<pcl::PointNormal, pcl::_PointNormal>
                (pyNspcl, "PointNormal", "* \\brief A point structure representing Euclidean xyz coordinates, together with normal coordinates and the surface curvature estimate. (SSE friendly)\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointNormal &>(),
                py::arg("p"))
            .def(py::init<float>(),
                py::arg("_curvature") = 0.f)
            .def(py::init<float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"))
            .def(py::init<float, float, float, float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("n_x"), py::arg("n_y"), py::arg("n_z"), py::arg("_curvature") = 0.f)
            ;


        auto pyNspcl_Class_PointXYZRGBNormal =
            py::class_<pcl::_PointXYZRGBNormal>
                (pyNspcl, "_PointXYZRGBNormal", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointXYZRGBNormal =
            py::class_<pcl::PointXYZRGBNormal, pcl::_PointXYZRGBNormal>
                (pyNspcl, "PointXYZRGBNormal", "* \\brief A point structure representing Euclidean xyz coordinates, and the RGB color, together with normal coordinates and the surface curvature estimate.\n    * Due to historical reasons (PCL was first developed as a ROS package), the\n    * RGB information is packed into an integer and casted to a float. This is\n    * something we wish to remove in the near future, but in the meantime, the\n    * following code snippet should help you pack and unpack RGB colors in your\n    * PointXYZRGB structure:\n    *\n    * \\code\n    * // pack r/g/b into rgb\n    * std::uint8_t r = 255, g = 0, b = 0;    // Example: Red color\n    * std::uint32_t rgb = ((std::uint32_t)r << 16 | (std::uint32_t)g << 8 | (std::uint32_t)b);\n    * p.rgb = *reinterpret_cast<float*>(&rgb);\n    * \\endcode\n    *\n    * To unpack the data into separate values, use:\n    *\n    * \\code\n    * PointXYZRGB p;\n    * // unpack rgb into r/g/b\n    * std::uint32_t rgb = *reinterpret_cast<int*>(&p.rgb);\n    * std::uint8_t r = (rgb >> 16) & 0x0000;\n    * std::uint8_t g = (rgb >> 8)  & 0x0000;\n    * std::uint8_t b = (rgb)       & 0x0000;\n    * \\endcode\n    *\n    *\n    * Alternatively, from 1.1.0 onwards, you can use p.r, p.g, and p.b directly.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointXYZRGBNormal &>(),
                py::arg("p"))
            .def(py::init<float>(),
                py::arg("_curvature") = 0.f)
            .def(py::init<float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"))
            .def(py::init<std::uint8_t, std::uint8_t, std::uint8_t>(),
                py::arg("_r"), py::arg("_g"), py::arg("_b"))
            .def(py::init<float, float, float, std::uint8_t, std::uint8_t, std::uint8_t>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_r"), py::arg("_g"), py::arg("_b"))
            .def(py::init<float, float, float, std::uint8_t, std::uint8_t, std::uint8_t, float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_r"), py::arg("_g"), py::arg("_b"), py::arg("n_x"), py::arg("n_y"), py::arg("n_z"), py::arg("_curvature") = 0.f)
            .def(py::init<float, float, float, std::uint8_t, std::uint8_t, std::uint8_t, std::uint8_t, float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_r"), py::arg("_g"), py::arg("_b"), py::arg("_a"), py::arg("n_x"), py::arg("n_y"), py::arg("n_z"), py::arg("_curvature") = 0.f)
            ;


        auto pyNspcl_Class_PointXYZINormal =
            py::class_<pcl::_PointXYZINormal>
                (pyNspcl, "_PointXYZINormal", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointXYZINormal =
            py::class_<pcl::PointXYZINormal, pcl::_PointXYZINormal>
                (pyNspcl, "PointXYZINormal", "* \\brief A point structure representing Euclidean xyz coordinates, intensity, together with normal coordinates and the surface curvature estimate.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointXYZINormal &>(),
                py::arg("p"))
            .def(py::init<float>(),
                py::arg("_intensity") = 0.f)
            .def(py::init<float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_intensity") = 0.f)
            .def(py::init<float, float, float, float, float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_intensity"), py::arg("n_x"), py::arg("n_y"), py::arg("n_z"), py::arg("_curvature") = 0.f)
            ;


        auto pyNspcl_Class_PointXYZLNormal =
            py::class_<pcl::_PointXYZLNormal>
                (pyNspcl, "_PointXYZLNormal", "----")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointXYZLNormal =
            py::class_<pcl::PointXYZLNormal, pcl::_PointXYZLNormal>
                (pyNspcl, "PointXYZLNormal", "* \\brief A point structure representing Euclidean xyz coordinates, a label, together with normal coordinates and the surface curvature estimate.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointXYZLNormal &>(),
                py::arg("p"))
            .def(py::init<std::uint32_t>(),
                py::arg("_label") = 0u)
            .def(py::init<float, float, float, std::uint32_t>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_label") = 0u)
            .def(py::init<float, float, float, std::uint32_t, float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_label"), py::arg("n_x"), py::arg("n_y"), py::arg("n_z"), py::arg("_curvature") = 0.f)
            ;


        auto pyNspcl_Class_PointWithRange =
            py::class_<pcl::_PointWithRange>
                (pyNspcl, "_PointWithRange", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointWithRange =
            py::class_<pcl::PointWithRange, pcl::_PointWithRange>
                (pyNspcl, "PointWithRange", "* \\brief A point structure representing Euclidean xyz coordinates, padded with an extra range float.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointWithRange &>(),
                py::arg("p"))
            .def(py::init<float>(),
                py::arg("_range") = 0.f)
            .def(py::init<float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_range") = 0.f)
            ;


        auto pyNspcl_Class_PointWithViewpoint =
            py::class_<pcl::_PointWithViewpoint>
                (pyNspcl, "_PointWithViewpoint", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointWithViewpoint =
            py::class_<pcl::PointWithViewpoint, pcl::_PointWithViewpoint>
                (pyNspcl, "PointWithViewpoint", "* \\brief A point structure representing Euclidean xyz coordinates together with the viewpoint from which it was seen.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointWithViewpoint &>(),
                py::arg("p"))
            .def(py::init<>())
            .def(py::init<float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"))
            .def(py::init<float, float, float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_vp_x"), py::arg("_vp_y"), py::arg("_vp_z"))
            ;


        auto pyNspcl_ClassMomentInvariants =
            py::class_<pcl::MomentInvariants>
                (pyNspcl, "MomentInvariants", "* \\brief A point structure representing the three moment invariants.\n    * \\ingroup common\n")
            .def_readwrite("j1", &pcl::MomentInvariants::j1, "")
            .def_readwrite("j2", &pcl::MomentInvariants::j2, "")
            .def_readwrite("j3", &pcl::MomentInvariants::j3, "")
            .def(py::init<>())
            .def(py::init<float, float, float>(),
                py::arg("_j1"), py::arg("_j2"), py::arg("_j3"))
            ;


        auto pyNspcl_ClassPrincipalRadiiRSD =
            py::class_<pcl::PrincipalRadiiRSD>
                (pyNspcl, "PrincipalRadiiRSD", "* \\brief A point structure representing the minimum and maximum surface radii (in meters) computed using RSD.\n    * \\ingroup common\n")
            .def_readwrite("r_min", &pcl::PrincipalRadiiRSD::r_min, "")
            .def_readwrite("r_max", &pcl::PrincipalRadiiRSD::r_max, "")
            .def(py::init<>())
            .def(py::init<float, float>(),
                py::arg("_r_min"), py::arg("_r_max"))
            ;


        auto pyNspcl_ClassBoundary =
            py::class_<pcl::Boundary>
                (pyNspcl, "Boundary", "* \\brief A point structure representing a description of whether a point is lying on a surface boundary or not.\n    * \\ingroup common\n")
            .def_readwrite("boundary_point", &pcl::Boundary::boundary_point, "")
            .def(py::init<std::uint8_t>(),
                py::arg("_boundary") = 0)
            ;


        auto pyNspcl_ClassPrincipalCurvatures =
            py::class_<pcl::PrincipalCurvatures>
                (pyNspcl, "PrincipalCurvatures", "* \\brief A point structure representing the principal curvatures and their magnitudes.\n    * \\ingroup common\n")
            .def_readwrite("pc1", &pcl::PrincipalCurvatures::pc1, "")
            .def_readwrite("pc2", &pcl::PrincipalCurvatures::pc2, "")
            .def(py::init<>())
            .def(py::init<float, float>(),
                py::arg("_pc1"), py::arg("_pc2"))
            .def(py::init<float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"))
            .def(py::init<float, float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_pc1"), py::arg("_pc2"))
            ;


        auto pyNspcl_ClassPFHSignature125 =
            py::class_<pcl::PFHSignature125>
                (pyNspcl, "PFHSignature125", "* \\brief A point structure representing the Point Feature Histogram (PFH).\n    * \\ingroup common\n")
            .def_property("histogram",
                [](pcl::PFHSignature125 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {125}, {sizeof(float)});
                    return pybind11::array(dtype, {125}, {sizeof(float)}, self.histogram, base);
                }, [](pcl::PFHSignature125& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::PFHSignature125::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassPFHRGBSignature250 =
            py::class_<pcl::PFHRGBSignature250>
                (pyNspcl, "PFHRGBSignature250", "* \\brief A point structure representing the Point Feature Histogram with colors (PFHRGB).\n    * \\ingroup common\n")
            .def_property("histogram",
                [](pcl::PFHRGBSignature250 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {250}, {sizeof(float)});
                    return pybind11::array(dtype, {250}, {sizeof(float)}, self.histogram, base);
                }, [](pcl::PFHRGBSignature250& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::PFHRGBSignature250::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassPPFSignature =
            py::class_<pcl::PPFSignature>
                (pyNspcl, "PPFSignature", "* \\brief A point structure for storing the Point Pair Feature (PPF) values\n    * \\ingroup common\n")
            .def_readwrite("f1", &pcl::PPFSignature::f1, "")
            .def_readwrite("f2", &pcl::PPFSignature::f2, "")
            .def_readwrite("f3", &pcl::PPFSignature::f3, "")
            .def_readwrite("f4", &pcl::PPFSignature::f4, "")
            .def_readwrite("alpha_m", &pcl::PPFSignature::alpha_m, "")
            .def(py::init<float>(),
                py::arg("_alpha") = 0.f)
            .def(py::init<float, float, float, float, float>(),
                py::arg("_f1"), py::arg("_f2"), py::arg("_f3"), py::arg("_f4"), py::arg("_alpha") = 0.f)
            ;


        auto pyNspcl_ClassCPPFSignature =
            py::class_<pcl::CPPFSignature>
                (pyNspcl, "CPPFSignature", "* \\brief A point structure for storing the Point Pair Feature (CPPF) values\n    * \\ingroup common\n")
            .def_readwrite("f1", &pcl::CPPFSignature::f1, "")
            .def_readwrite("f2", &pcl::CPPFSignature::f2, "")
            .def_readwrite("f3", &pcl::CPPFSignature::f3, "")
            .def_readwrite("f4", &pcl::CPPFSignature::f4, "")
            .def_readwrite("f5", &pcl::CPPFSignature::f5, "")
            .def_readwrite("f6", &pcl::CPPFSignature::f6, "")
            .def_readwrite("f7", &pcl::CPPFSignature::f7, "")
            .def_readwrite("f8", &pcl::CPPFSignature::f8, "")
            .def_readwrite("f9", &pcl::CPPFSignature::f9, "")
            .def_readwrite("f10", &pcl::CPPFSignature::f10, "")
            .def_readwrite("alpha_m", &pcl::CPPFSignature::alpha_m, "")
            .def(py::init<float>(),
                py::arg("_alpha") = 0.f)
            .def(py::init<float, float, float, float, float, float, float, float, float, float, float>(),
                py::arg("_f1"), py::arg("_f2"), py::arg("_f3"), py::arg("_f4"), py::arg("_f5"), py::arg("_f6"), py::arg("_f7"), py::arg("_f8"), py::arg("_f9"), py::arg("_f10"), py::arg("_alpha") = 0.f)
            ;


        auto pyNspcl_ClassPPFRGBSignature =
            py::class_<pcl::PPFRGBSignature>
                (pyNspcl, "PPFRGBSignature", "* \\brief A point structure for storing the Point Pair Color Feature (PPFRGB) values\n    * \\ingroup common\n")
            .def_readwrite("f1", &pcl::PPFRGBSignature::f1, "")
            .def_readwrite("f2", &pcl::PPFRGBSignature::f2, "")
            .def_readwrite("f3", &pcl::PPFRGBSignature::f3, "")
            .def_readwrite("f4", &pcl::PPFRGBSignature::f4, "")
            .def_readwrite("r_ratio", &pcl::PPFRGBSignature::r_ratio, "")
            .def_readwrite("g_ratio", &pcl::PPFRGBSignature::g_ratio, "")
            .def_readwrite("b_ratio", &pcl::PPFRGBSignature::b_ratio, "")
            .def_readwrite("alpha_m", &pcl::PPFRGBSignature::alpha_m, "")
            .def(py::init<float>(),
                py::arg("_alpha") = 0.f)
            .def(py::init<float, float, float, float, float>(),
                py::arg("_f1"), py::arg("_f2"), py::arg("_f3"), py::arg("_f4"), py::arg("_alpha") = 0.f)
            .def(py::init<float, float, float, float, float, float, float, float>(),
                py::arg("_f1"), py::arg("_f2"), py::arg("_f3"), py::arg("_f4"), py::arg("_alpha"), py::arg("_r"), py::arg("_g"), py::arg("_b"))
            ;


        auto pyNspcl_ClassNormalBasedSignature12 =
            py::class_<pcl::NormalBasedSignature12>
                (pyNspcl, "NormalBasedSignature12", "* \\brief A point structure representing the Normal Based Signature for\n    * a feature matrix of 4-by-3\n    * \\ingroup common\n")
            .def_property("values",
                [](pcl::NormalBasedSignature12 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {12}, {sizeof(float)});
                    return pybind11::array(dtype, {12}, {sizeof(float)}, self.values, base);
                }, [](pcl::NormalBasedSignature12& self) {},
                "")
            .def(py::init<>())
            ;


        auto pyNspcl_ClassShapeContext1980 =
            py::class_<pcl::ShapeContext1980>
                (pyNspcl, "ShapeContext1980", "* \\brief A point structure representing a Shape Context.\n    * \\ingroup common\n")
            .def_property("descriptor",
                [](pcl::ShapeContext1980 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {1980}, {sizeof(float)});
                    return pybind11::array(dtype, {1980}, {sizeof(float)}, self.descriptor, base);
                }, [](pcl::ShapeContext1980& self) {},
                "")
            .def_property("rf",
                [](pcl::ShapeContext1980 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {9}, {sizeof(float)});
                    return pybind11::array(dtype, {9}, {sizeof(float)}, self.rf, base);
                }, [](pcl::ShapeContext1980& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::ShapeContext1980::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassUniqueShapeContext1960 =
            py::class_<pcl::UniqueShapeContext1960>
                (pyNspcl, "UniqueShapeContext1960", "* \\brief A point structure representing a Unique Shape Context.\n    * \\ingroup common\n")
            .def_property("descriptor",
                [](pcl::UniqueShapeContext1960 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {1960}, {sizeof(float)});
                    return pybind11::array(dtype, {1960}, {sizeof(float)}, self.descriptor, base);
                }, [](pcl::UniqueShapeContext1960& self) {},
                "")
            .def_property("rf",
                [](pcl::UniqueShapeContext1960 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {9}, {sizeof(float)});
                    return pybind11::array(dtype, {9}, {sizeof(float)}, self.rf, base);
                }, [](pcl::UniqueShapeContext1960& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::UniqueShapeContext1960::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassSHOT352 =
            py::class_<pcl::SHOT352>
                (pyNspcl, "SHOT352", "* \\brief A point structure representing the generic Signature of Histograms of OrienTations (SHOT) - shape only.\n    * \\ingroup common\n")
            .def_property("descriptor",
                [](pcl::SHOT352 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {352}, {sizeof(float)});
                    return pybind11::array(dtype, {352}, {sizeof(float)}, self.descriptor, base);
                }, [](pcl::SHOT352& self) {},
                "")
            .def_property("rf",
                [](pcl::SHOT352 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {9}, {sizeof(float)});
                    return pybind11::array(dtype, {9}, {sizeof(float)}, self.rf, base);
                }, [](pcl::SHOT352& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::SHOT352::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassSHOT1344 =
            py::class_<pcl::SHOT1344>
                (pyNspcl, "SHOT1344", "* \\brief A point structure representing the generic Signature of Histograms of OrienTations (SHOT) - shape+color.\n    * \\ingroup common\n")
            .def_property("descriptor",
                [](pcl::SHOT1344 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {1344}, {sizeof(float)});
                    return pybind11::array(dtype, {1344}, {sizeof(float)}, self.descriptor, base);
                }, [](pcl::SHOT1344& self) {},
                "")
            .def_property("rf",
                [](pcl::SHOT1344 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {9}, {sizeof(float)});
                    return pybind11::array(dtype, {9}, {sizeof(float)}, self.rf, base);
                }, [](pcl::SHOT1344& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::SHOT1344::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_Class_ReferenceFrame =
            py::class_<pcl::_ReferenceFrame>
                (pyNspcl, "_ReferenceFrame", "* \\brief A structure representing the Local Reference Frame of a point.\n    *  \\ingroup common\n")
            .def(py::init<>()) // implicit default constructor
            .def("get_x_axis_vector3f_map",
                [](pcl::_ReferenceFrame & self) { return self.getXAxisVector3fMap(); })
            .def("get_x_axis_vector3f_map",
                [](pcl::_ReferenceFrame & self) { return self.getXAxisVector3fMap(); })
            .def("get_y_axis_vector3f_map",
                [](pcl::_ReferenceFrame & self) { return self.getYAxisVector3fMap(); })
            .def("get_y_axis_vector3f_map",
                [](pcl::_ReferenceFrame & self) { return self.getYAxisVector3fMap(); })
            .def("get_z_axis_vector3f_map",
                [](pcl::_ReferenceFrame & self) { return self.getZAxisVector3fMap(); })
            .def("get_z_axis_vector3f_map",
                [](pcl::_ReferenceFrame & self) { return self.getZAxisVector3fMap(); })
            .def("get_matrix3f_map",
                [](pcl::_ReferenceFrame & self) { return self.getMatrix3fMap(); })
            .def("get_matrix3f_map",
                [](pcl::_ReferenceFrame & self) { return self.getMatrix3fMap(); })
            ;


        auto pyNspcl_ClassReferenceFrame =
            py::class_<pcl::ReferenceFrame, pcl::_ReferenceFrame>
                (pyNspcl, "ReferenceFrame", "")
            .def(py::init<const pcl::_ReferenceFrame &>(),
                py::arg("p"))
            .def(py::init<>())
            ;


        auto pyNspcl_ClassFPFHSignature33 =
            py::class_<pcl::FPFHSignature33>
                (pyNspcl, "FPFHSignature33", "* \\brief A point structure representing the Fast Point Feature Histogram (FPFH).\n    * \\ingroup common\n")
            .def_property("histogram",
                [](pcl::FPFHSignature33 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {33}, {sizeof(float)});
                    return pybind11::array(dtype, {33}, {sizeof(float)}, self.histogram, base);
                }, [](pcl::FPFHSignature33& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::FPFHSignature33::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassVFHSignature308 =
            py::class_<pcl::VFHSignature308>
                (pyNspcl, "VFHSignature308", "* \\brief A point structure representing the Viewpoint Feature Histogram (VFH).\n    * \\ingroup common\n")
            .def_property("histogram",
                [](pcl::VFHSignature308 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {308}, {sizeof(float)});
                    return pybind11::array(dtype, {308}, {sizeof(float)}, self.histogram, base);
                }, [](pcl::VFHSignature308& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::VFHSignature308::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassGRSDSignature21 =
            py::class_<pcl::GRSDSignature21>
                (pyNspcl, "GRSDSignature21", "* \\brief A point structure representing the Global Radius-based Surface Descriptor (GRSD).\n    * \\ingroup common\n")
            .def_property("histogram",
                [](pcl::GRSDSignature21 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {21}, {sizeof(float)});
                    return pybind11::array(dtype, {21}, {sizeof(float)}, self.histogram, base);
                }, [](pcl::GRSDSignature21& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::GRSDSignature21::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassBRISKSignature512 =
            py::class_<pcl::BRISKSignature512>
                (pyNspcl, "BRISKSignature512", "* \\brief A point structure representing the Binary Robust Invariant Scalable Keypoints (BRISK).\n    * \\ingroup common\n")
            .def_readwrite("scale", &pcl::BRISKSignature512::scale, "")
            .def_readwrite("orientation", &pcl::BRISKSignature512::orientation, "")
            .def_static("descriptor_size",
                &pcl::BRISKSignature512::descriptorSize)
            .def(py::init<>())
            .def(py::init<float, float>(),
                py::arg("_scale"), py::arg("_orientation"))
            ;


        auto pyNspcl_ClassESFSignature640 =
            py::class_<pcl::ESFSignature640>
                (pyNspcl, "ESFSignature640", "* \\brief A point structure representing the Ensemble of Shape Functions (ESF).\n    * \\ingroup common\n")
            .def_property("histogram",
                [](pcl::ESFSignature640 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {640}, {sizeof(float)});
                    return pybind11::array(dtype, {640}, {sizeof(float)}, self.histogram, base);
                }, [](pcl::ESFSignature640& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::ESFSignature640::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassGASDSignature512 =
            py::class_<pcl::GASDSignature512>
                (pyNspcl, "GASDSignature512", "* \\brief A point structure representing the Globally Aligned Spatial Distribution (GASD) shape descriptor.\n  * \\ingroup common\n")
            .def_property("histogram",
                [](pcl::GASDSignature512 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {512}, {sizeof(float)});
                    return pybind11::array(dtype, {512}, {sizeof(float)}, self.histogram, base);
                }, [](pcl::GASDSignature512& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::GASDSignature512::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassGASDSignature984 =
            py::class_<pcl::GASDSignature984>
                (pyNspcl, "GASDSignature984", "* \\brief A point structure representing the Globally Aligned Spatial Distribution (GASD) shape and color descriptor.\n  * \\ingroup common\n")
            .def_property("histogram",
                [](pcl::GASDSignature984 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {984}, {sizeof(float)});
                    return pybind11::array(dtype, {984}, {sizeof(float)}, self.histogram, base);
                }, [](pcl::GASDSignature984& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::GASDSignature984::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassGASDSignature7992 =
            py::class_<pcl::GASDSignature7992>
                (pyNspcl, "GASDSignature7992", "* \\brief A point structure representing the Globally Aligned Spatial Distribution (GASD) shape and color descriptor.\n  * \\ingroup common\n")
            .def_property("histogram",
                [](pcl::GASDSignature7992 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {7992}, {sizeof(float)});
                    return pybind11::array(dtype, {7992}, {sizeof(float)}, self.histogram, base);
                }, [](pcl::GASDSignature7992& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::GASDSignature7992::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassGFPFHSignature16 =
            py::class_<pcl::GFPFHSignature16>
                (pyNspcl, "GFPFHSignature16", "* \\brief A point structure representing the GFPFH descriptor with 16 bins.\n    * \\ingroup common\n")
            .def_property("histogram",
                [](pcl::GFPFHSignature16 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {16}, {sizeof(float)});
                    return pybind11::array(dtype, {16}, {sizeof(float)}, self.histogram, base);
                }, [](pcl::GFPFHSignature16& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::GFPFHSignature16::descriptorSize)
            .def(py::init<>())
            ;


        auto pyNspcl_ClassNarf36 =
            py::class_<pcl::Narf36>
                (pyNspcl, "Narf36", "* \\brief A point structure representing the Narf descriptor.\n    * \\ingroup common\n")
            .def_readwrite("x", &pcl::Narf36::x, "")
            .def_readwrite("y", &pcl::Narf36::y, "")
            .def_readwrite("z", &pcl::Narf36::z, "")
            .def_readwrite("roll", &pcl::Narf36::roll, "")
            .def_readwrite("pitch", &pcl::Narf36::pitch, "")
            .def_readwrite("yaw", &pcl::Narf36::yaw, "")
            .def_property("descriptor",
                [](pcl::Narf36 &self) -> pybind11::array
                {
                    auto dtype = pybind11::dtype(pybind11::format_descriptor<float>::format());
                    auto base = pybind11::array(dtype, {36}, {sizeof(float)});
                    return pybind11::array(dtype, {36}, {sizeof(float)}, self.descriptor, base);
                }, [](pcl::Narf36& self) {},
                "")
            .def_static("descriptor_size",
                &pcl::Narf36::descriptorSize)
            .def(py::init<>())
            .def(py::init<float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"))
            .def(py::init<float, float, float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_roll"), py::arg("_pitch"), py::arg("_yaw"))
            ;


        auto pyNspcl_ClassBorderDescription =
            py::class_<pcl::BorderDescription>
                (pyNspcl, "BorderDescription", "* \\brief A structure to store if a point in a range image lies on a border between an obstacle and the background.\n    * \\ingroup common\n")
            .def_readwrite("x", &pcl::BorderDescription::x, "")
            .def_readwrite("y", &pcl::BorderDescription::y, "")
            .def_readwrite("traits", &pcl::BorderDescription::traits, "")
            .def(py::init<>())
            .def(py::init<int, int>(),
                py::arg("_x"), py::arg("_y"))
            ;


        auto pyNspcl_ClassIntensityGradient =
            py::class_<pcl::IntensityGradient>
                (pyNspcl, "IntensityGradient", "* \\brief A point structure representing the intensity gradient of an XYZI point cloud.\n    * \\ingroup common\n")
            .def(py::init<>())
            .def(py::init<float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"))
            ;


        auto pyNspcl_Class_PointWithScale =
            py::class_<pcl::_PointWithScale>
                (pyNspcl, "_PointWithScale", "")
            .def(py::init<>([](
            float angle = float(), float response = float(), int octave = int())
            {
                auto r = std::make_unique<pcl::_PointWithScale>();
                r->angle = angle;
                r->response = response;
                r->octave = octave;
                return r;
            })
            , py::arg("angle") = float(), py::arg("response") = float(), py::arg("octave") = int()
            )
            .def_readwrite("angle", &pcl::_PointWithScale::angle, "* \\brief Computed orientation of the keypoint (-1 if not applicable).")
            .def_readwrite("response", &pcl::_PointWithScale::response, "* \\brief The response by which the most strong keypoints have been selected.")
            .def_readwrite("octave", &pcl::_PointWithScale::octave, "* \\brief octave (pyramid layer) from which the keypoint has been extracted.")
            ;


        auto pyNspcl_ClassPointWithScale =
            py::class_<pcl::PointWithScale, pcl::_PointWithScale>
                (pyNspcl, "PointWithScale", "* \\brief A point structure representing a 3-D position and scale.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointWithScale &>(),
                py::arg("p"))
            .def(py::init<>())
            .def(py::init<float, float, float, float, float, float, int>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_scale") = 1.f, py::arg("_angle") = -1.f, py::arg("_response") = 0.f, py::arg("_octave") = 0)
            ;


        auto pyNspcl_Class_PointSurfel =
            py::class_<pcl::_PointSurfel>
                (pyNspcl, "_PointSurfel", "")
            .def(py::init<>()) // implicit default constructor
            ;


        auto pyNspcl_ClassPointSurfel =
            py::class_<pcl::PointSurfel, pcl::_PointSurfel>
                (pyNspcl, "PointSurfel", "* \\brief A surfel, that is, a point structure representing Euclidean xyz coordinates, together with normal coordinates, a RGBA color, a radius, a confidence value and the surface curvature estimate.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointSurfel &>(),
                py::arg("p"))
            .def(py::init<>())
            .def(py::init<float, float, float, float, float, float, std::uint8_t, std::uint8_t, std::uint8_t, std::uint8_t, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_nx"), py::arg("_ny"), py::arg("_nz"), py::arg("_r"), py::arg("_g"), py::arg("_b"), py::arg("_a"), py::arg("_radius"), py::arg("_confidence"), py::arg("_curvature"))
            ;


        auto pyNspcl_Class_PointDEM =
            py::class_<pcl::_PointDEM>
                (pyNspcl, "_PointDEM", "")
            .def(py::init<>([](
            float intensity_variance = float(), float height_variance = float())
            {
                auto r = std::make_unique<pcl::_PointDEM>();
                r->intensity_variance = intensity_variance;
                r->height_variance = height_variance;
                return r;
            })
            , py::arg("intensity_variance") = float(), py::arg("height_variance") = float()
            )
            .def_readwrite("intensity", &pcl::_PointDEM::intensity, "")
            .def_readwrite("intensity_variance", &pcl::_PointDEM::intensity_variance, "")
            .def_readwrite("height_variance", &pcl::_PointDEM::height_variance, "")
            ;


        auto pyNspcl_ClassPointDEM =
            py::class_<pcl::PointDEM, pcl::_PointDEM>
                (pyNspcl, "PointDEM", "* \\brief A point structure representing Digital Elevation Map.\n    * \\ingroup common\n")
            .def(py::init<const pcl::_PointDEM &>(),
                py::arg("p"))
            .def(py::init<>())
            .def(py::init<float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"))
            .def(py::init<float, float, float, float, float, float>(),
                py::arg("_x"), py::arg("_y"), py::arg("_z"), py::arg("_intensity"), py::arg("_intensity_variance"), py::arg("_height_variance"))
            ;

        { // <namespace traits>
            py::module_ pyNspcl_Nstraits = pyNspcl.def_submodule("traits", "");
        } // </namespace traits>

    } // </namespace pcl>
    ////////////////////    </generated_from:point_types.hpp>    ////////////////////


    ////////////////////    <generated_from:PointIndices.h>    ////////////////////

    { // <namespace pcl>
        py::module_ pyNspcl = m.def_submodule("pcl", "namespace pcl");
        auto pyNspcl_ClassPointIndices =
            py::class_<pcl::PointIndices>
                (pyNspcl, "PointIndices", "struct PointIndices")
            .def(py::init<>())
            .def_readwrite("header", &pcl::PointIndices::header, "")
            .def_readwrite("indices", &pcl::PointIndices::indices, "")
            ;
    } // </namespace pcl>
    ////////////////////    </generated_from:PointIndices.h>    ////////////////////


    ////////////////////    <generated_from:point_cloud.h>    ////////////////////

    { // <namespace pcl>
        py::module_ pyNspcl = m.def_submodule("pcl", "");
        auto pyNspcl_ClassPointCloud_pcl_PointXYZ =
            py::class_<pcl::PointCloud<pcl::PointXYZ>>
                (pyNspcl, "PointCloud_pcl_PointXYZ", "* \\brief PointCloud represents the base class in PCL for storing collections of 3D points.\n    *\n    * The class is templated, which means you need to specify the type of data\n    * that it should contain. For example, to create a point cloud that holds 4\n    * random XYZ data points, use:\n    *\n    * \\code\n    * pcl::PointCloud<pcl::PointXYZ> cloud;\n    * cloud.push_back (pcl::PointXYZ (rand (), rand (), rand ()));\n    * cloud.push_back (pcl::PointXYZ (rand (), rand (), rand ()));\n    * cloud.push_back (pcl::PointXYZ (rand (), rand (), rand ()));\n    * cloud.push_back (pcl::PointXYZ (rand (), rand (), rand ()));\n    * \\endcode\n    *\n    * The PointCloud class contains the following elements:\n    *   - \\b width - specifies the width of the point cloud dataset in the number of points. WIDTH has two meanings:\n    *     - it can specify the total number of points in the cloud (equal with POINTS see below) for unorganized datasets;\n    *     - it can specify the width (total number of points in a row) of an organized point cloud dataset.\n    *   \\a Mandatory.\n    *   - \\b height - specifies the height of the point cloud dataset in the number of points. HEIGHT has two meanings:\n    *     - it can specify the height (total number of rows) of an organized point cloud dataset;\n    *     - it is set to 1 for unorganized datasets (thus used to check whether a dataset is organized or not).\n    *   \\a Mandatory.\n    *   - \\b points - the data array where all points of type <b>PointT</b> are stored. \\a Mandatory.\n    *\n    *   - \\b is_dense - specifies if all the data in <b>points</b> is finite (True), or whether it might contain Inf/NaN values\n    * (False). \\a Mandatory.\n    *\n    *   - \\b sensor_origin_ - specifies the sensor acquisition pose (origin/translation). \\a Optional.\n    *   - \\b sensor_orientation_ - specifies the sensor acquisition pose (rotation). \\a Optional.\n    *\n    * \\author Patrick Mihelich, Radu B. Rusu\n")
            .def(py::init<>(),
                "* \\brief Default constructor. Sets \\ref is_dense to True, \\ref width\n        * and \\ref height to 0, and the \\ref sensor_origin_ and \\ref\n        * sensor_orientation_ to identity.\n")
            .def(py::init<const pcl::PointCloud<pcl::PointXYZ> &, const Indices &>(),
                py::arg("pc"), py::arg("indices"),
                "* \\brief Copy constructor from point cloud subset\n        * \\param[in] pc the cloud to copy into this\n        * \\param[in] indices the subset to copy\n")
            .def(py::init<std::uint32_t, std::uint32_t, const pcl::PointXYZ &>(),
                py::arg("width_"), py::arg("height_"), py::arg("value_") = PointT (),
                "* \\brief Allocate constructor from point cloud subset\n        * \\param[in] width_ the cloud width\n        * \\param[in] height_ the cloud height\n        * \\param[in] value_ default value\n")
            .def("__iadd__",
                &pcl::PointCloud::operator+=,
                py::arg("rhs"),
                "* \\brief Add a point cloud to the current cloud.\n        * \\param[in] rhs the cloud to add to the current cloud\n        * \\return the new cloud as a concatenation of the current cloud and the new given cloud\n")
            .def("__add__",
                &pcl::PointCloud::operator+,
                py::arg("rhs"),
                "* \\brief Add a point cloud to another cloud.\n        * \\param[in] rhs the cloud to add to the current cloud\n        * \\return the new cloud as a concatenation of the current cloud and the new given cloud\n")
            .def_static("concatenate",
                py::overload_cast<pcl::PointCloud<pcl::PointXYZ> &, const pcl::PointCloud<pcl::PointXYZ> &>(&pcl::PointCloud<pcl::PointXYZ>::concatenate), py::arg("cloud1"), py::arg("cloud2"))
            .def_static("concatenate",
                py::overload_cast<const pcl::PointCloud<pcl::PointXYZ> &, const pcl::PointCloud<pcl::PointXYZ> &, pcl::PointCloud<pcl::PointXYZ> &>(&pcl::PointCloud<pcl::PointXYZ>::concatenate), py::arg("cloud1"), py::arg("cloud2"), py::arg("cloud_out"))
            .def("at",
                py::overload_cast<int, int>(&pcl::PointCloud<pcl::PointXYZ>::at, py::const_),
                py::arg("column"), py::arg("row"),
                "* \\brief Obtain the point given by the (column, row) coordinates. Only works on organized\n        * datasets (those that have height != 1).\n        * \\param[in] column the column coordinate\n        * \\param[in] row the row coordinate\n")
            .def("at",
                py::overload_cast<int, int>(&pcl::PointCloud<pcl::PointXYZ>::at),
                py::arg("column"), py::arg("row"),
                "* \\brief Obtain the point given by the (column, row) coordinates. Only works on organized\n        * datasets (those that have height != 1).\n        * \\param[in] column the column coordinate\n        * \\param[in] row the row coordinate\n")
            .def("__call__",
                py::overload_cast<std::size_t, std::size_t>(&pcl::PointCloud<pcl::PointXYZ>::operator(), py::const_),
                py::arg("column"), py::arg("row"),
                "* \\brief Obtain the point given by the (column, row) coordinates. Only works on organized\n        * datasets (those that have height != 1).\n        * \\param[in] column the column coordinate\n        * \\param[in] row the row coordinate\n")
            .def("__call__",
                py::overload_cast<std::size_t, std::size_t>(&pcl::PointCloud<pcl::PointXYZ>::operator()),
                py::arg("column"), py::arg("row"),
                "* \\brief Obtain the point given by the (column, row) coordinates. Only works on organized\n        * datasets (those that have height != 1).\n        * \\param[in] column the column coordinate\n        * \\param[in] row the row coordinate\n")
            .def("is_organized",
                &pcl::PointCloud<pcl::PointXYZ>::isOrganized, "* \\brief Return whether a dataset is organized (e.g., arranged in a structured grid).\n        * \note The height value must be different than 1 for a dataset to be organized.\n")
            .def("get_matrix_xf_map",
                py::overload_cast<int, int, int>(&pcl::PointCloud<pcl::PointXYZ>::getMatrixXfMap),
                py::arg("dim"), py::arg("stride"), py::arg("offset"),
                "* \\brief Return an Eigen MatrixXf (assumes float values) mapped to the specified dimensions of the PointCloud.\n        * \note This method is for advanced users only! Use with care!\n        *\n        * \\attention Compile time flags used for Eigen might affect the dimension of the Eigen::Map returned. If Eigen\n        *   is using row major storage, the matrix shape would be (number of Points X elements in a Point) else\n        *   the matrix shape would be (elements in a Point X number of Points). Essentially,\n        *   * Major direction: number of points in cloud\n        *   * Minor direction: number of point dimensions\n        * By default, as of Eigen 3.3, Eigen uses Column major storage\n        *\n        * \\param[in] dim the number of dimensions to consider for each point\n        * \\param[in] stride the number of values in each point (will be the number of values that separate two of the columns)\n        * \\param[in] offset the number of dimensions to skip from the beginning of each point\n        *            (stride = offset + dim + x, where x is the number of dimensions to skip from the end of each point)\n        * \note for getting only XYZ coordinates out of PointXYZ use dim=3, stride=4 and offset=0 due to the alignment.\n        * \\attention PointT types are most of the time aligned, so the offsets are not continuous!\n")
            .def("get_matrix_xf_map",
                py::overload_cast<int, int, int>(&pcl::PointCloud<pcl::PointXYZ>::getMatrixXfMap, py::const_),
                py::arg("dim"), py::arg("stride"), py::arg("offset"),
                "* \\brief Return an Eigen MatrixXf (assumes float values) mapped to the specified dimensions of the PointCloud.\n        * \note This method is for advanced users only! Use with care!\n        *\n        * \\attention Compile time flags used for Eigen might affect the dimension of the Eigen::Map returned. If Eigen\n        *   is using row major storage, the matrix shape would be (number of Points X elements in a Point) else\n        *   the matrix shape would be (elements in a Point X number of Points). Essentially,\n        *   * Major direction: number of points in cloud\n        *   * Minor direction: number of point dimensions\n        * By default, as of Eigen 3.3, Eigen uses Column major storage\n        *\n        * \\param[in] dim the number of dimensions to consider for each point\n        * \\param[in] stride the number of values in each point (will be the number of values that separate two of the columns)\n        * \\param[in] offset the number of dimensions to skip from the beginning of each point\n        *            (stride = offset + dim + x, where x is the number of dimensions to skip from the end of each point)\n        * \note for getting only XYZ coordinates out of PointXYZ use dim=3, stride=4 and offset=0 due to the alignment.\n        * \\attention PointT types are most of the time aligned, so the offsets are not continuous!\n")
            .def("get_matrix_xf_map",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.getMatrixXfMap(); }, "*\n       * \\brief Return an Eigen MatrixXf (assumes float values) mapped to the PointCloud.\n       * \note This method is for advanced users only! Use with care!\n       * \\attention PointT types are most of the time aligned, so the offsets are not continuous!\n       * \overload Eigen::Map<Eigen::MatrixXf, Eigen::Aligned, Eigen::OuterStride<> > pcl::PointCloud::getMatrixXfMap ()\n")
            .def("get_matrix_xf_map",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.getMatrixXfMap(); }, "*\n       * \\brief Return an Eigen MatrixXf (assumes float values) mapped to the PointCloud.\n       * \note This method is for advanced users only! Use with care!\n       * \\attention PointT types are most of the time aligned, so the offsets are not continuous!\n       * \overload const Eigen::Map<Eigen::MatrixXf, Eigen::Aligned, Eigen::OuterStride<> > pcl::PointCloud::getMatrixXfMap () const\n")
            .def_readwrite("header", &pcl::PointCloud<pcl::PointXYZ>::header, "* \\brief The point cloud header. It contains information about the acquisition time.")
            .def_readwrite("points", &pcl::PointCloud<pcl::PointXYZ>::points, "* \\brief The point data.")
            .def_readwrite("width", &pcl::PointCloud<pcl::PointXYZ>::width, "* \\brief The point cloud width (if organized as an image-structure).")
            .def_readwrite("height", &pcl::PointCloud<pcl::PointXYZ>::height, "* \\brief The point cloud height (if organized as an image-structure).")
            .def_readwrite("is_dense", &pcl::PointCloud<pcl::PointXYZ>::is_dense, "* \\brief True if no points are invalid (e.g., have NaN or Inf values in any of their floating point fields).")
            .def_readwrite("sensor_origin_", &pcl::PointCloud<pcl::PointXYZ>::sensor_origin_, "* \\brief Sensor acquisition pose (origin/translation).")
            .def_readwrite("sensor_orientation_", &pcl::PointCloud<pcl::PointXYZ>::sensor_orientation_, "* \\brief Sensor acquisition pose (rotation).")
            .def("begin",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.begin(); })
            .def("end",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.end(); })
            .def("begin",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.begin(); })
            .def("end",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.end(); })
            .def("cbegin",
                &pcl::PointCloud<pcl::PointXYZ>::cbegin)
            .def("cend",
                &pcl::PointCloud<pcl::PointXYZ>::cend)
            .def("rbegin",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.rbegin(); })
            .def("rend",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.rend(); })
            .def("rbegin",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.rbegin(); })
            .def("rend",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.rend(); })
            .def("crbegin",
                &pcl::PointCloud<pcl::PointXYZ>::crbegin)
            .def("crend",
                &pcl::PointCloud<pcl::PointXYZ>::crend)
            .def("size",
                &pcl::PointCloud<pcl::PointXYZ>::size)
            .def("max_size",
                &pcl::PointCloud<pcl::PointXYZ>::max_size)
            .def("reserve",
                &pcl::PointCloud<pcl::PointXYZ>::reserve, py::arg("n"))
            .def("empty",
                &pcl::PointCloud<pcl::PointXYZ>::empty)
            .def("data",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.data(); })
            .def("data",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.data(); })
            .def("resize",
                py::overload_cast<std::size_t>(&pcl::PointCloud<pcl::PointXYZ>::resize),
                py::arg("count"),
                "*\n       * \\brief Resizes the container to contain `count` elements\n       * \\details\n       * * If the current size is greater than `count`, the pointcloud is reduced to its\n       * first `count` elements\n       * * If the current size is less than `count`, additional default-inserted points\n       * are appended\n       * \note This potentially breaks the organized structure of the cloud by setting\n       * the height to 1 IFF `width * height != count`!\n       * \\param[in] count new size of the point cloud\n")
            .def("resize",
                py::overload_cast<uindex_t, uindex_t>(&pcl::PointCloud<pcl::PointXYZ>::resize),
                py::arg("new_width"), py::arg("new_height"),
                "*\n       * \\brief Resizes the container to contain `new_width * new_height` elements\n       * \\details\n       * * If the current size is greater than the requested size, the pointcloud\n       * is reduced to its first requested elements\n       * * If the current size is less then the requested size, additional\n       * default-inserted points are appended\n       * \\param[in] new_width new width of the point cloud\n       * \\param[in] new_height new height of the point cloud\n")
            .def("resize",
                py::overload_cast<index_t, const pcl::PointXYZ &>(&pcl::PointCloud<pcl::PointXYZ>::resize),
                py::arg("count"), py::arg("value"),
                "*\n       * \\brief Resizes the container to contain count elements\n       * \\details\n       * * If the current size is greater than `count`, the pointcloud is reduced to its\n       * first `count` elements\n       * * If the current size is less than `count`, additional copies of `value` are\n       * appended\n       * \note This potentially breaks the organized structure of the cloud by setting\n       * the height to 1 IFF `width * height != count`!\n       * \\param[in] count new size of the point cloud\n       * \\param[in] value the value to initialize the new points with\n")
            .def("resize",
                py::overload_cast<index_t, index_t, const pcl::PointXYZ &>(&pcl::PointCloud<pcl::PointXYZ>::resize),
                py::arg("new_width"), py::arg("new_height"), py::arg("value"),
                "*\n       * \\brief Resizes the container to contain count elements\n       * \\details\n       * * If the current size is greater then the requested size, the pointcloud\n       * is reduced to its first requested elements\n       * * If the current size is less then the requested size, additional\n       * default-inserted points are appended\n       * \\param[in] new_width new width of the point cloud\n       * \\param[in] new_height new height of the point cloud\n       * \\param[in] value the value to initialize the new points with\n")
            .def("__getitem__",
                py::overload_cast<std::size_t>(&pcl::PointCloud<pcl::PointXYZ>::operator[], py::const_), py::arg("n"))
            .def("__getitem__",
                py::overload_cast<std::size_t>(&pcl::PointCloud<pcl::PointXYZ>::operator[]), py::arg("n"))
            .def("at",
                py::overload_cast<std::size_t>(&pcl::PointCloud<pcl::PointXYZ>::at, py::const_), py::arg("n"))
            .def("at",
                py::overload_cast<std::size_t>(&pcl::PointCloud<pcl::PointXYZ>::at), py::arg("n"))
            .def("front",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.front(); })
            .def("front",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.front(); })
            .def("back",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.back(); })
            .def("back",
                [](pcl::PointCloud<pcl::PointXYZ> & self) { return self.back(); })
            .def("assign",
                py::overload_cast<index_t, const pcl::PointXYZ &>(&pcl::PointCloud<pcl::PointXYZ>::assign),
                py::arg("count"), py::arg("value"),
                "*\n       * \\brief Replaces the points with `count` copies of `value`\n       * \note This breaks the organized structure of the cloud by setting the height to\n       * 1!\n       * \\param[in] count new size of the point cloud\n       * \\param[in] value value each point of the cloud should have\n")
            .def("assign",
                py::overload_cast<index_t, index_t, const pcl::PointXYZ &>(&pcl::PointCloud<pcl::PointXYZ>::assign),
                py::arg("new_width"), py::arg("new_height"), py::arg("value"),
                "*\n       * \\brief Replaces the points with `new_width * new_height` copies of `value`\n       * \\param[in] new_width new width of the point cloud\n       * \\param[in] new_height new height of the point cloud\n       * \\param[in] value value each point of the cloud should have\n")
            .def("assign",
                py::overload_cast<std::initializer_list<pcl::PointXYZ>>(&pcl::PointCloud<pcl::PointXYZ>::assign),
                py::arg("ilist"),
                "*\n       * \\brief Replaces the points with the elements from the initializer list `ilist`\n       * \note This breaks the organized structure of the cloud by setting the height to\n       * 1!\n")
            .def("assign",
                py::overload_cast<std::initializer_list<pcl::PointXYZ>, index_t>(&pcl::PointCloud<pcl::PointXYZ>::assign),
                py::arg("ilist"), py::arg("new_width"),
                "*\n       * \\brief Replaces the points with the elements from the initializer list `ilist`\n       * \note This calculates the height based on size and width provided. This means\n       * the assignment happens even if the size is not perfectly divisible by width\n       * \\param[in] ilist initializer list from which the points are copied\n       * \\param[in] new_width new width of the point cloud\n")
            .def("push_back",
                &pcl::PointCloud<pcl::PointXYZ>::push_back,
                py::arg("pt"),
                "* \\brief Insert a new point in the cloud, at the end of the container.\n        * \note This breaks the organized structure of the cloud by setting the height to 1!\n        * \\param[in] pt the point to insert\n")
            .def("transient_push_back",
                &pcl::PointCloud<pcl::PointXYZ>::transient_push_back,
                py::arg("pt"),
                "* \\brief Insert a new point in the cloud, at the end of the container.\n        * \note This assumes the user would correct the width and height later on!\n        * \\param[in] pt the point to insert\n")
            .def("insert",
                py::overload_cast<iterator, const pcl::PointXYZ &>(&pcl::PointCloud<pcl::PointXYZ>::insert),
                py::arg("position"), py::arg("pt"),
                "* \\brief Insert a new point in the cloud, given an iterator.\n        * \note This breaks the organized structure of the cloud by setting the height to 1!\n        * \\param[in] position where to insert the point\n        * \\param[in] pt the point to insert\n        * \\return returns the new position iterator\n")
            .def("transient_insert",
                py::overload_cast<iterator, const pcl::PointXYZ &>(&pcl::PointCloud<pcl::PointXYZ>::transient_insert),
                py::arg("position"), py::arg("pt"),
                "* \\brief Insert a new point in the cloud, given an iterator.\n        * \note This assumes the user would correct the width and height later on!\n        * \\param[in] position where to insert the point\n        * \\param[in] pt the point to insert\n        * \\return returns the new position iterator\n")
            .def("insert",
                py::overload_cast<iterator, std::size_t, const pcl::PointXYZ &>(&pcl::PointCloud<pcl::PointXYZ>::insert),
                py::arg("position"), py::arg("n"), py::arg("pt"),
                "* \\brief Insert a new point in the cloud N times, given an iterator.\n        * \note This breaks the organized structure of the cloud by setting the height to 1!\n        * \\param[in] position where to insert the point\n        * \\param[in] n the number of times to insert the point\n        * \\param[in] pt the point to insert\n")
            .def("transient_insert",
                py::overload_cast<iterator, std::size_t, const pcl::PointXYZ &>(&pcl::PointCloud<pcl::PointXYZ>::transient_insert),
                py::arg("position"), py::arg("n"), py::arg("pt"),
                "* \\brief Insert a new point in the cloud N times, given an iterator.\n        * \note This assumes the user would correct the width and height later on!\n        * \\param[in] position where to insert the point\n        * \\param[in] n the number of times to insert the point\n        * \\param[in] pt the point to insert\n")
            .def("erase",
                py::overload_cast<iterator>(&pcl::PointCloud<pcl::PointXYZ>::erase),
                py::arg("position"),
                "* \\brief Erase a point in the cloud.\n        * \note This breaks the organized structure of the cloud by setting the height to 1!\n        * \\param[in] position what data point to erase\n        * \\return returns the new position iterator\n")
            .def("transient_erase",
                py::overload_cast<iterator>(&pcl::PointCloud<pcl::PointXYZ>::transient_erase),
                py::arg("position"),
                "* \\brief Erase a point in the cloud.\n        * \note This assumes the user would correct the width and height later on!\n        * \\param[in] position what data point to erase\n        * \\return returns the new position iterator\n")
            .def("erase",
                py::overload_cast<iterator, iterator>(&pcl::PointCloud<pcl::PointXYZ>::erase),
                py::arg("first"), py::arg("last"),
                "* \\brief Erase a set of points given by a (first, last) iterator pair\n        * \note This breaks the organized structure of the cloud by setting the height to 1!\n        * \\param[in] first where to start erasing points from\n        * \\param[in] last where to stop erasing points from\n        * \\return returns the new position iterator\n")
            .def("transient_erase",
                py::overload_cast<iterator, iterator>(&pcl::PointCloud<pcl::PointXYZ>::transient_erase),
                py::arg("first"), py::arg("last"),
                "* \\brief Erase a set of points given by a (first, last) iterator pair\n        * \note This assumes the user would correct the width and height later on!\n        * \\param[in] first where to start erasing points from\n        * \\param[in] last where to stop erasing points from\n        * \\return returns the new position iterator\n")
            .def("swap",
                &pcl::PointCloud<pcl::PointXYZ>::swap,
                py::arg("rhs"),
                "* \\brief Swap a point cloud with another cloud.\n        * \\param[in,out] rhs point cloud to swap this with\n")
            .def("clear",
                &pcl::PointCloud<pcl::PointXYZ>::clear, "* \\brief Removes all points in a cloud and sets the width and height to 0.")
            .def("make_shared",
                &pcl::PointCloud<pcl::PointXYZ>::makeShared, "* \\brief Copy the cloud to the heap and return a smart pointer\n        * Note that deep copy is performed, so avoid using this function on non-empty clouds.\n        * The changes of the returned cloud are not mirrored back to this one.\n        * \\return shared pointer to the copy of the cloud\n")
            ;
        auto pyNspcl_ClassPointCloud_pcl_PointXYZRGBA =
            py::class_<pcl::PointCloud<pcl::PointXYZRGBA>>
                (pyNspcl, "PointCloud_pcl_PointXYZRGBA", "* \\brief PointCloud represents the base class in PCL for storing collections of 3D points.\n    *\n    * The class is templated, which means you need to specify the type of data\n    * that it should contain. For example, to create a point cloud that holds 4\n    * random XYZ data points, use:\n    *\n    * \\code\n    * pcl::PointCloud<pcl::PointXYZ> cloud;\n    * cloud.push_back (pcl::PointXYZ (rand (), rand (), rand ()));\n    * cloud.push_back (pcl::PointXYZ (rand (), rand (), rand ()));\n    * cloud.push_back (pcl::PointXYZ (rand (), rand (), rand ()));\n    * cloud.push_back (pcl::PointXYZ (rand (), rand (), rand ()));\n    * \\endcode\n    *\n    * The PointCloud class contains the following elements:\n    *   - \\b width - specifies the width of the point cloud dataset in the number of points. WIDTH has two meanings:\n    *     - it can specify the total number of points in the cloud (equal with POINTS see below) for unorganized datasets;\n    *     - it can specify the width (total number of points in a row) of an organized point cloud dataset.\n    *   \\a Mandatory.\n    *   - \\b height - specifies the height of the point cloud dataset in the number of points. HEIGHT has two meanings:\n    *     - it can specify the height (total number of rows) of an organized point cloud dataset;\n    *     - it is set to 1 for unorganized datasets (thus used to check whether a dataset is organized or not).\n    *   \\a Mandatory.\n    *   - \\b points - the data array where all points of type <b>PointT</b> are stored. \\a Mandatory.\n    *\n    *   - \\b is_dense - specifies if all the data in <b>points</b> is finite (True), or whether it might contain Inf/NaN values\n    * (False). \\a Mandatory.\n    *\n    *   - \\b sensor_origin_ - specifies the sensor acquisition pose (origin/translation). \\a Optional.\n    *   - \\b sensor_orientation_ - specifies the sensor acquisition pose (rotation). \\a Optional.\n    *\n    * \\author Patrick Mihelich, Radu B. Rusu\n")
            .def(py::init<>(),
                "* \\brief Default constructor. Sets \\ref is_dense to True, \\ref width\n        * and \\ref height to 0, and the \\ref sensor_origin_ and \\ref\n        * sensor_orientation_ to identity.\n")
            .def(py::init<const pcl::PointCloud<pcl::PointXYZRGBA> &, const Indices &>(),
                py::arg("pc"), py::arg("indices"),
                "* \\brief Copy constructor from point cloud subset\n        * \\param[in] pc the cloud to copy into this\n        * \\param[in] indices the subset to copy\n")
            .def(py::init<std::uint32_t, std::uint32_t, const pcl::PointXYZRGBA &>(),
                py::arg("width_"), py::arg("height_"), py::arg("value_") = PointT (),
                "* \\brief Allocate constructor from point cloud subset\n        * \\param[in] width_ the cloud width\n        * \\param[in] height_ the cloud height\n        * \\param[in] value_ default value\n")
            .def("__iadd__",
                &pcl::PointCloud::operator+=,
                py::arg("rhs"),
                "* \\brief Add a point cloud to the current cloud.\n        * \\param[in] rhs the cloud to add to the current cloud\n        * \\return the new cloud as a concatenation of the current cloud and the new given cloud\n")
            .def("__add__",
                &pcl::PointCloud::operator+,
                py::arg("rhs"),
                "* \\brief Add a point cloud to another cloud.\n        * \\param[in] rhs the cloud to add to the current cloud\n        * \\return the new cloud as a concatenation of the current cloud and the new given cloud\n")
            .def_static("concatenate",
                py::overload_cast<pcl::PointCloud<pcl::PointXYZRGBA> &, const pcl::PointCloud<pcl::PointXYZRGBA> &>(&pcl::PointCloud<pcl::PointXYZRGBA>::concatenate), py::arg("cloud1"), py::arg("cloud2"))
            .def_static("concatenate",
                py::overload_cast<const pcl::PointCloud<pcl::PointXYZRGBA> &, const pcl::PointCloud<pcl::PointXYZRGBA> &, pcl::PointCloud<pcl::PointXYZRGBA> &>(&pcl::PointCloud<pcl::PointXYZRGBA>::concatenate), py::arg("cloud1"), py::arg("cloud2"), py::arg("cloud_out"))
            .def("at",
                py::overload_cast<int, int>(&pcl::PointCloud<pcl::PointXYZRGBA>::at, py::const_),
                py::arg("column"), py::arg("row"),
                "* \\brief Obtain the point given by the (column, row) coordinates. Only works on organized\n        * datasets (those that have height != 1).\n        * \\param[in] column the column coordinate\n        * \\param[in] row the row coordinate\n")
            .def("at",
                py::overload_cast<int, int>(&pcl::PointCloud<pcl::PointXYZRGBA>::at),
                py::arg("column"), py::arg("row"),
                "* \\brief Obtain the point given by the (column, row) coordinates. Only works on organized\n        * datasets (those that have height != 1).\n        * \\param[in] column the column coordinate\n        * \\param[in] row the row coordinate\n")
            .def("__call__",
                py::overload_cast<std::size_t, std::size_t>(&pcl::PointCloud<pcl::PointXYZRGBA>::operator(), py::const_),
                py::arg("column"), py::arg("row"),
                "* \\brief Obtain the point given by the (column, row) coordinates. Only works on organized\n        * datasets (those that have height != 1).\n        * \\param[in] column the column coordinate\n        * \\param[in] row the row coordinate\n")
            .def("__call__",
                py::overload_cast<std::size_t, std::size_t>(&pcl::PointCloud<pcl::PointXYZRGBA>::operator()),
                py::arg("column"), py::arg("row"),
                "* \\brief Obtain the point given by the (column, row) coordinates. Only works on organized\n        * datasets (those that have height != 1).\n        * \\param[in] column the column coordinate\n        * \\param[in] row the row coordinate\n")
            .def("is_organized",
                &pcl::PointCloud<pcl::PointXYZRGBA>::isOrganized, "* \\brief Return whether a dataset is organized (e.g., arranged in a structured grid).\n        * \note The height value must be different than 1 for a dataset to be organized.\n")
            .def("get_matrix_xf_map",
                py::overload_cast<int, int, int>(&pcl::PointCloud<pcl::PointXYZRGBA>::getMatrixXfMap),
                py::arg("dim"), py::arg("stride"), py::arg("offset"),
                "* \\brief Return an Eigen MatrixXf (assumes float values) mapped to the specified dimensions of the PointCloud.\n        * \note This method is for advanced users only! Use with care!\n        *\n        * \\attention Compile time flags used for Eigen might affect the dimension of the Eigen::Map returned. If Eigen\n        *   is using row major storage, the matrix shape would be (number of Points X elements in a Point) else\n        *   the matrix shape would be (elements in a Point X number of Points). Essentially,\n        *   * Major direction: number of points in cloud\n        *   * Minor direction: number of point dimensions\n        * By default, as of Eigen 3.3, Eigen uses Column major storage\n        *\n        * \\param[in] dim the number of dimensions to consider for each point\n        * \\param[in] stride the number of values in each point (will be the number of values that separate two of the columns)\n        * \\param[in] offset the number of dimensions to skip from the beginning of each point\n        *            (stride = offset + dim + x, where x is the number of dimensions to skip from the end of each point)\n        * \note for getting only XYZ coordinates out of PointXYZ use dim=3, stride=4 and offset=0 due to the alignment.\n        * \\attention PointT types are most of the time aligned, so the offsets are not continuous!\n")
            .def("get_matrix_xf_map",
                py::overload_cast<int, int, int>(&pcl::PointCloud<pcl::PointXYZRGBA>::getMatrixXfMap, py::const_),
                py::arg("dim"), py::arg("stride"), py::arg("offset"),
                "* \\brief Return an Eigen MatrixXf (assumes float values) mapped to the specified dimensions of the PointCloud.\n        * \note This method is for advanced users only! Use with care!\n        *\n        * \\attention Compile time flags used for Eigen might affect the dimension of the Eigen::Map returned. If Eigen\n        *   is using row major storage, the matrix shape would be (number of Points X elements in a Point) else\n        *   the matrix shape would be (elements in a Point X number of Points). Essentially,\n        *   * Major direction: number of points in cloud\n        *   * Minor direction: number of point dimensions\n        * By default, as of Eigen 3.3, Eigen uses Column major storage\n        *\n        * \\param[in] dim the number of dimensions to consider for each point\n        * \\param[in] stride the number of values in each point (will be the number of values that separate two of the columns)\n        * \\param[in] offset the number of dimensions to skip from the beginning of each point\n        *            (stride = offset + dim + x, where x is the number of dimensions to skip from the end of each point)\n        * \note for getting only XYZ coordinates out of PointXYZ use dim=3, stride=4 and offset=0 due to the alignment.\n        * \\attention PointT types are most of the time aligned, so the offsets are not continuous!\n")
            .def("get_matrix_xf_map",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.getMatrixXfMap(); }, "*\n       * \\brief Return an Eigen MatrixXf (assumes float values) mapped to the PointCloud.\n       * \note This method is for advanced users only! Use with care!\n       * \\attention PointT types are most of the time aligned, so the offsets are not continuous!\n       * \overload Eigen::Map<Eigen::MatrixXf, Eigen::Aligned, Eigen::OuterStride<> > pcl::PointCloud::getMatrixXfMap ()\n")
            .def("get_matrix_xf_map",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.getMatrixXfMap(); }, "*\n       * \\brief Return an Eigen MatrixXf (assumes float values) mapped to the PointCloud.\n       * \note This method is for advanced users only! Use with care!\n       * \\attention PointT types are most of the time aligned, so the offsets are not continuous!\n       * \overload const Eigen::Map<Eigen::MatrixXf, Eigen::Aligned, Eigen::OuterStride<> > pcl::PointCloud::getMatrixXfMap () const\n")
            .def_readwrite("header", &pcl::PointCloud<pcl::PointXYZRGBA>::header, "* \\brief The point cloud header. It contains information about the acquisition time.")
            .def_readwrite("points", &pcl::PointCloud<pcl::PointXYZRGBA>::points, "* \\brief The point data.")
            .def_readwrite("width", &pcl::PointCloud<pcl::PointXYZRGBA>::width, "* \\brief The point cloud width (if organized as an image-structure).")
            .def_readwrite("height", &pcl::PointCloud<pcl::PointXYZRGBA>::height, "* \\brief The point cloud height (if organized as an image-structure).")
            .def_readwrite("is_dense", &pcl::PointCloud<pcl::PointXYZRGBA>::is_dense, "* \\brief True if no points are invalid (e.g., have NaN or Inf values in any of their floating point fields).")
            .def_readwrite("sensor_origin_", &pcl::PointCloud<pcl::PointXYZRGBA>::sensor_origin_, "* \\brief Sensor acquisition pose (origin/translation).")
            .def_readwrite("sensor_orientation_", &pcl::PointCloud<pcl::PointXYZRGBA>::sensor_orientation_, "* \\brief Sensor acquisition pose (rotation).")
            .def("begin",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.begin(); })
            .def("end",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.end(); })
            .def("begin",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.begin(); })
            .def("end",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.end(); })
            .def("cbegin",
                &pcl::PointCloud<pcl::PointXYZRGBA>::cbegin)
            .def("cend",
                &pcl::PointCloud<pcl::PointXYZRGBA>::cend)
            .def("rbegin",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.rbegin(); })
            .def("rend",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.rend(); })
            .def("rbegin",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.rbegin(); })
            .def("rend",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.rend(); })
            .def("crbegin",
                &pcl::PointCloud<pcl::PointXYZRGBA>::crbegin)
            .def("crend",
                &pcl::PointCloud<pcl::PointXYZRGBA>::crend)
            .def("size",
                &pcl::PointCloud<pcl::PointXYZRGBA>::size)
            .def("max_size",
                &pcl::PointCloud<pcl::PointXYZRGBA>::max_size)
            .def("reserve",
                &pcl::PointCloud<pcl::PointXYZRGBA>::reserve, py::arg("n"))
            .def("empty",
                &pcl::PointCloud<pcl::PointXYZRGBA>::empty)
            .def("data",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.data(); })
            .def("data",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.data(); })
            .def("resize",
                py::overload_cast<std::size_t>(&pcl::PointCloud<pcl::PointXYZRGBA>::resize),
                py::arg("count"),
                "*\n       * \\brief Resizes the container to contain `count` elements\n       * \\details\n       * * If the current size is greater than `count`, the pointcloud is reduced to its\n       * first `count` elements\n       * * If the current size is less than `count`, additional default-inserted points\n       * are appended\n       * \note This potentially breaks the organized structure of the cloud by setting\n       * the height to 1 IFF `width * height != count`!\n       * \\param[in] count new size of the point cloud\n")
            .def("resize",
                py::overload_cast<uindex_t, uindex_t>(&pcl::PointCloud<pcl::PointXYZRGBA>::resize),
                py::arg("new_width"), py::arg("new_height"),
                "*\n       * \\brief Resizes the container to contain `new_width * new_height` elements\n       * \\details\n       * * If the current size is greater than the requested size, the pointcloud\n       * is reduced to its first requested elements\n       * * If the current size is less then the requested size, additional\n       * default-inserted points are appended\n       * \\param[in] new_width new width of the point cloud\n       * \\param[in] new_height new height of the point cloud\n")
            .def("resize",
                py::overload_cast<index_t, const pcl::PointXYZRGBA &>(&pcl::PointCloud<pcl::PointXYZRGBA>::resize),
                py::arg("count"), py::arg("value"),
                "*\n       * \\brief Resizes the container to contain count elements\n       * \\details\n       * * If the current size is greater than `count`, the pointcloud is reduced to its\n       * first `count` elements\n       * * If the current size is less than `count`, additional copies of `value` are\n       * appended\n       * \note This potentially breaks the organized structure of the cloud by setting\n       * the height to 1 IFF `width * height != count`!\n       * \\param[in] count new size of the point cloud\n       * \\param[in] value the value to initialize the new points with\n")
            .def("resize",
                py::overload_cast<index_t, index_t, const pcl::PointXYZRGBA &>(&pcl::PointCloud<pcl::PointXYZRGBA>::resize),
                py::arg("new_width"), py::arg("new_height"), py::arg("value"),
                "*\n       * \\brief Resizes the container to contain count elements\n       * \\details\n       * * If the current size is greater then the requested size, the pointcloud\n       * is reduced to its first requested elements\n       * * If the current size is less then the requested size, additional\n       * default-inserted points are appended\n       * \\param[in] new_width new width of the point cloud\n       * \\param[in] new_height new height of the point cloud\n       * \\param[in] value the value to initialize the new points with\n")
            .def("__getitem__",
                py::overload_cast<std::size_t>(&pcl::PointCloud<pcl::PointXYZRGBA>::operator[], py::const_), py::arg("n"))
            .def("__getitem__",
                py::overload_cast<std::size_t>(&pcl::PointCloud<pcl::PointXYZRGBA>::operator[]), py::arg("n"))
            .def("at",
                py::overload_cast<std::size_t>(&pcl::PointCloud<pcl::PointXYZRGBA>::at, py::const_), py::arg("n"))
            .def("at",
                py::overload_cast<std::size_t>(&pcl::PointCloud<pcl::PointXYZRGBA>::at), py::arg("n"))
            .def("front",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.front(); })
            .def("front",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.front(); })
            .def("back",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.back(); })
            .def("back",
                [](pcl::PointCloud<pcl::PointXYZRGBA> & self) { return self.back(); })
            .def("assign",
                py::overload_cast<index_t, const pcl::PointXYZRGBA &>(&pcl::PointCloud<pcl::PointXYZRGBA>::assign),
                py::arg("count"), py::arg("value"),
                "*\n       * \\brief Replaces the points with `count` copies of `value`\n       * \note This breaks the organized structure of the cloud by setting the height to\n       * 1!\n       * \\param[in] count new size of the point cloud\n       * \\param[in] value value each point of the cloud should have\n")
            .def("assign",
                py::overload_cast<index_t, index_t, const pcl::PointXYZRGBA &>(&pcl::PointCloud<pcl::PointXYZRGBA>::assign),
                py::arg("new_width"), py::arg("new_height"), py::arg("value"),
                "*\n       * \\brief Replaces the points with `new_width * new_height` copies of `value`\n       * \\param[in] new_width new width of the point cloud\n       * \\param[in] new_height new height of the point cloud\n       * \\param[in] value value each point of the cloud should have\n")
            .def("assign",
                py::overload_cast<std::initializer_list<pcl::PointXYZRGBA>>(&pcl::PointCloud<pcl::PointXYZRGBA>::assign),
                py::arg("ilist"),
                "*\n       * \\brief Replaces the points with the elements from the initializer list `ilist`\n       * \note This breaks the organized structure of the cloud by setting the height to\n       * 1!\n")
            .def("assign",
                py::overload_cast<std::initializer_list<pcl::PointXYZRGBA>, index_t>(&pcl::PointCloud<pcl::PointXYZRGBA>::assign),
                py::arg("ilist"), py::arg("new_width"),
                "*\n       * \\brief Replaces the points with the elements from the initializer list `ilist`\n       * \note This calculates the height based on size and width provided. This means\n       * the assignment happens even if the size is not perfectly divisible by width\n       * \\param[in] ilist initializer list from which the points are copied\n       * \\param[in] new_width new width of the point cloud\n")
            .def("push_back",
                &pcl::PointCloud<pcl::PointXYZRGBA>::push_back,
                py::arg("pt"),
                "* \\brief Insert a new point in the cloud, at the end of the container.\n        * \note This breaks the organized structure of the cloud by setting the height to 1!\n        * \\param[in] pt the point to insert\n")
            .def("transient_push_back",
                &pcl::PointCloud<pcl::PointXYZRGBA>::transient_push_back,
                py::arg("pt"),
                "* \\brief Insert a new point in the cloud, at the end of the container.\n        * \note This assumes the user would correct the width and height later on!\n        * \\param[in] pt the point to insert\n")
            .def("insert",
                py::overload_cast<iterator, const pcl::PointXYZRGBA &>(&pcl::PointCloud<pcl::PointXYZRGBA>::insert),
                py::arg("position"), py::arg("pt"),
                "* \\brief Insert a new point in the cloud, given an iterator.\n        * \note This breaks the organized structure of the cloud by setting the height to 1!\n        * \\param[in] position where to insert the point\n        * \\param[in] pt the point to insert\n        * \\return returns the new position iterator\n")
            .def("transient_insert",
                py::overload_cast<iterator, const pcl::PointXYZRGBA &>(&pcl::PointCloud<pcl::PointXYZRGBA>::transient_insert),
                py::arg("position"), py::arg("pt"),
                "* \\brief Insert a new point in the cloud, given an iterator.\n        * \note This assumes the user would correct the width and height later on!\n        * \\param[in] position where to insert the point\n        * \\param[in] pt the point to insert\n        * \\return returns the new position iterator\n")
            .def("insert",
                py::overload_cast<iterator, std::size_t, const pcl::PointXYZRGBA &>(&pcl::PointCloud<pcl::PointXYZRGBA>::insert),
                py::arg("position"), py::arg("n"), py::arg("pt"),
                "* \\brief Insert a new point in the cloud N times, given an iterator.\n        * \note This breaks the organized structure of the cloud by setting the height to 1!\n        * \\param[in] position where to insert the point\n        * \\param[in] n the number of times to insert the point\n        * \\param[in] pt the point to insert\n")
            .def("transient_insert",
                py::overload_cast<iterator, std::size_t, const pcl::PointXYZRGBA &>(&pcl::PointCloud<pcl::PointXYZRGBA>::transient_insert),
                py::arg("position"), py::arg("n"), py::arg("pt"),
                "* \\brief Insert a new point in the cloud N times, given an iterator.\n        * \note This assumes the user would correct the width and height later on!\n        * \\param[in] position where to insert the point\n        * \\param[in] n the number of times to insert the point\n        * \\param[in] pt the point to insert\n")
            .def("erase",
                py::overload_cast<iterator>(&pcl::PointCloud<pcl::PointXYZRGBA>::erase),
                py::arg("position"),
                "* \\brief Erase a point in the cloud.\n        * \note This breaks the organized structure of the cloud by setting the height to 1!\n        * \\param[in] position what data point to erase\n        * \\return returns the new position iterator\n")
            .def("transient_erase",
                py::overload_cast<iterator>(&pcl::PointCloud<pcl::PointXYZRGBA>::transient_erase),
                py::arg("position"),
                "* \\brief Erase a point in the cloud.\n        * \note This assumes the user would correct the width and height later on!\n        * \\param[in] position what data point to erase\n        * \\return returns the new position iterator\n")
            .def("erase",
                py::overload_cast<iterator, iterator>(&pcl::PointCloud<pcl::PointXYZRGBA>::erase),
                py::arg("first"), py::arg("last"),
                "* \\brief Erase a set of points given by a (first, last) iterator pair\n        * \note This breaks the organized structure of the cloud by setting the height to 1!\n        * \\param[in] first where to start erasing points from\n        * \\param[in] last where to stop erasing points from\n        * \\return returns the new position iterator\n")
            .def("transient_erase",
                py::overload_cast<iterator, iterator>(&pcl::PointCloud<pcl::PointXYZRGBA>::transient_erase),
                py::arg("first"), py::arg("last"),
                "* \\brief Erase a set of points given by a (first, last) iterator pair\n        * \note This assumes the user would correct the width and height later on!\n        * \\param[in] first where to start erasing points from\n        * \\param[in] last where to stop erasing points from\n        * \\return returns the new position iterator\n")
            .def("swap",
                &pcl::PointCloud<pcl::PointXYZRGBA>::swap,
                py::arg("rhs"),
                "* \\brief Swap a point cloud with another cloud.\n        * \\param[in,out] rhs point cloud to swap this with\n")
            .def("clear",
                &pcl::PointCloud<pcl::PointXYZRGBA>::clear, "* \\brief Removes all points in a cloud and sets the width and height to 0.")
            .def("make_shared",
                &pcl::PointCloud<pcl::PointXYZRGBA>::makeShared, "* \\brief Copy the cloud to the heap and return a smart pointer\n        * Note that deep copy is performed, so avoid using this function on non-empty clouds.\n        * The changes of the returned cloud are not mirrored back to this one.\n        * \\return shared pointer to the copy of the cloud\n")
            ;
    } // </namespace pcl>
    ////////////////////    </generated_from:point_cloud.h>    ////////////////////

    // </litgen_pydef> // Autogenerated code end
    // !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!  AUTOGENERATED CODE END !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
}
